<h1><%= @cocktail.name %> Recipe</h1>
<% @doses.each do |dose| %>
    <li><%=dose.ingredient.name%> (<%=dose.description%>) <%= link_to "delete", dose_path(dose), method: :delete%></li>
<% end %>

<%= link_to 'Back', root_path %>

<%= render 'cocktails/dose-form', cocktail: @cocktail, dose: @dose %>